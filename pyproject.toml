[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "preictal"
version = "0.1.0"
description = "Cross-patient EEG seizure forecasting (preictal_classifier_v2)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
